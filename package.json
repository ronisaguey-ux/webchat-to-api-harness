{
  "name": "webchat-api",
  "version": "1.0.0",
  "description": "OpenAI/Anthropic-compatible API over a real webchat tab (Puppeteer) with tool-call support",
  "engines": {
    "node": ">=20.12"
  },
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check src/browser/browser.js && node --check src/tools/tools.js && node --check src/core/config.js && node --check src/runtime/rate_limit.js",
    "test": "node --test --test-concurrency=1 harness_tests/*.test.js",
    "webchat": "node bin/webchat.js",
    "test:unit": "node --test --test-concurrency=1 harness_tests/*.test.js",
    "test:tui": "node harness_tests/cli_smoke.js",
    "test:all": "npm run test && npm run test:tui"
  },
  "dependencies": {
    "express": "^4.18.2",
    "puppeteer": "^25.10.0",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1"
  },
  "bin": {
    "webchat": "./bin/webchat.js"
  }
}
