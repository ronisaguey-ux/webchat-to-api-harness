{
  "name": "webchat-api",
  "version": "1.0.0",
  "description": "OpenAI/Anthropic-compatible API over a real webchat tab (Puppeteer) with tool-call support",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check browser.js && node --check tools.js && node --check config.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "puppeteer": "^21.0.0",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1"
  }
}
