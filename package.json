{
  "name": "webchat-api",
  "version": "1.0.0",
  "description": "OpenAI/Anthropic-compatible API over a real webchat tab (Puppeteer) with tool-call support",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check browser.js && node --check tools.js && node --check config.js && node --check rate_limit.js",
    "test": "node --test harness_tests/*.test.js",
    "webchat": "node bin/webchat.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "puppeteer": "^25.10.0",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1"
  },
  "bin": {
    "webchat": "./bin/webchat.js"
  }
}
